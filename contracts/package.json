{
  "name": "mantleluxury-contracts",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy:mantle": "hardhat run scripts/deploy.ts --network mantleTestnet",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/node": "^22.0.0",
    "dotenv": "^17.2.3",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
